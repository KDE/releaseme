Please add <%= version %> to bugs.kde.org product <%= projectName %>

NOTE: PLEASE CHOOSE THE CORRECT TARGET AND REMOVE THE OTHER - either "version based style"

Target: <%= origin %>/<%= projectName %>/<%= version %>

NOTE: OR "flat style" - see https://download.kde.org/<%= origin %>/<%= projectName %> for current state

Target: <%= origin %>/<%= projectName %>

SHA-256:

```
<%= sha256s.join($/) %>
```

SHA-1:

```
<%= sha1s.join($/) %>
```
