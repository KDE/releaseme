Please add <%= version %> to bugs.kde.org product <%= projectName %>

NOTE: PLEASE CHOOSE THE CORRECT TARGET AND REMOVE THE OTHER - either "version based style"

Target: <%= target %>/<%= projectName %>/<%= version %>

NOTE: OR "flat style" - see https://download.kde.org/<%= target %>/<%= projectName %> for current state

Target: <%= target %>/<%= projectName %>

SHA-256:

```
<%= sha256s.join($/) %>
```

SHA-1:

```
<%= sha1s.join($/) %>
```
